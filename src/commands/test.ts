// import { Command, program } from 'commander';

// export const testCommand = new Command('test')
//   .description('A test command that runs a command')
//   .option('--name <name>', 'Name to greet')
//   .action((_, options) => {
//     console.log(options.name)
//   });
